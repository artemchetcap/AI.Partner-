# План 001: Инициализация проекта и MVP

## Цель (Objective)

Создать MVP бота БизнесGPT для соло-предпринимателей и консультантов с 4 ключевыми фичами:
1. 📝 КП-генератор (3 шага)
2. 💬 Помощь с перепиской в мессенджерах (3 шага)
3. 💡 Генератор идей (5 → +3)
4. ✨ Улучшение текста

**UX-принципы MVP:**
- Отложенный онбординг — сначала ценность
- Лимит 15 запросов/день — дать распробовать
- Унифицированные кнопки: [📋 Копировать] [🔄 Ещё] [🏠 Меню]

**Результат:** Работающий бот, который можно показать первым пользователям.

---

## Фаза 1: Структура проекта (День 1-2)

### 1.1 Создать структуру папок

```
dogs/
├── src/
│   └── bot/
│       ├── __init__.py
│       ├── main.py              # Точка входа
│       ├── config.py            # Конфигурация (Pydantic Settings)
│       ├── handlers/
│       │   ├── __init__.py
│       │   ├── start.py         # /start (без онбординга — сразу меню)
│       │   ├── menu.py          # Главное меню
│       │   ├── onboarding.py    # Отложенный онбординг (после 2-3 генераций)
│       │   ├── kp_generator.py  # 📝 КП-генератор
│       │   ├── messaging.py     # 💬 Помощь с перепиской
│       │   ├── ideas.py         # 💡 Генератор идей
│       │   └── improve.py       # ✨ Улучшение текста
│       ├── keyboards/
│       │   ├── __init__.py
│       │   ├── main_menu.py     # Главное меню
│       │   └── generation.py    # Клавиатуры для генерации
│       ├── services/
│       │   ├── __init__.py
│       │   ├── ai_service.py    # OpenAI интеграция
│       │   └── prompts.py       # Промпты для разных задач
│       ├── models/
│       │   ├── __init__.py
│       │   └── user.py          # Модель пользователя
│       ├── middlewares/
│       │   ├── __init__.py
│       │   └── user_middleware.py  # Регистрация пользователя
│       └── utils/
│           ├── __init__.py
│           └── text_utils.py    # Утилиты для текста
├── migrations/                   # Aerich миграции
├── tests/
│   └── __init__.py
├── .env.example
├── .gitignore
├── docker-compose.yml
├── Dockerfile
├── pyproject.toml
└── README.md
```

### 1.2 Настроить pyproject.toml

```toml
[project]
name = "business-gpt-bot"
version = "0.1.0"
requires-python = ">=3.11"

dependencies = [
    "aiogram>=3.4.0",
    "openai>=1.12.0",
    "tortoise-orm>=0.20.0",
    "asyncpg>=0.29.0",
    "redis>=5.0.0",
    "pydantic-settings>=2.1.0",
    "python-dotenv>=1.0.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
    "pytest-asyncio>=0.23.0",
    "black>=24.0.0",
    "ruff>=0.2.0",
]
```

### 1.3 Создать .env.example

```env
# Telegram
BOT_TOKEN=your_bot_token_here

# OpenAI
OPENAI_API_KEY=your_openai_key_here
OPENAI_MODEL=gpt-4o-mini

# Database
DATABASE_URL=postgres://user:password@localhost:5432/businessgpt

# Redis
REDIS_URL=redis://localhost:6379/0

# App
DEBUG=true
```

### 1.4 Настроить Docker

**docker-compose.yml:**
```yaml
services:
  bot:
    build: .
    env_file: .env
    depends_on:
      - db
      - redis
    volumes:
      - ./src:/app/src

  db:
    image: postgres:15
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: businessgpt
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    volumes:
      - redis_data:/data

volumes:
  postgres_data:
  redis_data:
```

**Критерии завершения Фазы 1:**
- [ ] Структура папок создана
- [ ] pyproject.toml с зависимостями
- [ ] .env.example готов
- [ ] docker-compose.yml готов
- [ ] Зависимости устанавливаются без ошибок

---

## Фаза 2: Базовый бот (День 3-4)

### 2.1 Конфигурация (config.py)

```python
from pydantic_settings import BaseSettings

class Settings(BaseSettings):
    bot_token: str
    openai_api_key: str
    openai_model: str = "gpt-4o-mini"
    database_url: str
    redis_url: str
    debug: bool = False

    class Config:
        env_file = ".env"
```

### 2.2 Точка входа (main.py)

- Инициализация бота и диспетчера aiogram
- Подключение к БД (Tortoise ORM)
- Регистрация handlers и middlewares
- Запуск polling (для dev) / webhook (для prod)

### 2.3 Модель пользователя (models/user.py)

```python
class User(Model):
    id = fields.BigIntField(pk=True)  # Telegram user_id
    username = fields.CharField(max_length=255, null=True)
    first_name = fields.CharField(max_length=255)
    
    # Контекст бизнеса (для персонализации)
    business_type = fields.CharField(max_length=100, null=True)  # "founder" / "consultant"
    business_niche = fields.CharField(max_length=255, null=True)
    
    # Статистика
    requests_today = fields.IntField(default=0)
    total_requests = fields.IntField(default=0)
    
    created_at = fields.DatetimeField(auto_now_add=True)
    updated_at = fields.DatetimeField(auto_now=True)
```

### 2.4 Хендлер /start (handlers/start.py)

**Флоу (отложенный онбординг):**
1. Приветствие + сразу показ главного меню
2. Пользователь сразу может использовать бота
3. После 2-3 генераций → триггер анкеты (в отдельном хендлере onboarding.py)

```
/start → "Привет! Попробуй прямо сейчас 👇" → [Меню с 4 кнопками]
```

> **Важно:** НЕ спрашиваем анкету при первом /start. Сначала даём ценность!

**Критерии завершения Фазы 2:**
- [ ] Бот запускается без ошибок
- [ ] /start сразу показывает меню (без анкеты)
- [ ] Пользователь сохраняется в БД
- [ ] Главное меню с 4 кнопками работает

---

## Фаза 3: AI-сервис и промпты (День 5-6)

### 3.1 AI Service (services/ai_service.py)

```python
class AIService:
    async def generate(self, prompt_key: str, user_context: dict, **kwargs) -> str:
        """Генерация текста через OpenAI по ключу промпта"""
        
    async def generate_kp(self, user: User, client: str, service: str, budget: str) -> str:
        """Генерация КП"""
        
    async def generate_message(self, user: User, situation: str, goal: str, tone: str) -> str:
        """Генерация сообщения для переписки"""
        
    async def generate_ideas(self, user: User, task: str, count: int = 5) -> str:
        """Генерация идей"""
        
    async def improve_text(self, user: User, text: str, style: str) -> str:
        """Улучшение текста"""
```

### 3.2 Промпты (services/prompts.py)

**Структура промптов:**

| Задача | System Prompt | User Prompt |
|--------|---------------|-------------|
| КП | "Ты эксперт по продажам..." | "Напиши КП: клиент {client}..." |
| Переписка | "Ты помощник по переписке..." | "Ситуация: {situation}, цель: {goal}" |
| Идеи | "Ты креативный консультант..." | "Дай {count} идей для: {task}..." |
| Улучшение | "Ты редактор текстов..." | "Улучши текст ({style}): {text}" |

> Детали промптов см. в `docs/prompts.md`

**Критерии завершения Фазы 3:**
- [ ] AIService подключается к OpenAI
- [ ] Все 4 метода генерации работают
- [ ] Промпты дают качественный результат
- [ ] Обработка ошибок API

---

## Фаза 4: Генерация контента (День 7-9)

### 4.1 Главное меню

```
Что делаем?
```

**Кнопки (2x2):**
```
[📝 Создать КП]           [💬 Помощь с перепиской]
[💡 Генератор идей]       [✨ Улучшить текст]
```

```
[⚙️ Настройки]  [❓ Помощь]
```

### 4.2 КП-генератор (handlers/kp_generator.py)

**Флоу с индикаторами шагов:**
1. Кнопка "📝 Создать КП"
2. **Шаг 1 из 3:** "Для кого КП?" + кнопки [🏢 Компания] [👤 Частное лицо]
3. **Шаг 2 из 3:** "Какая услуга?"
4. **Шаг 3 из 3:** "Бюджет?" + кнопка [⏭ Без бюджета]
5. Генерация (typing indicator)
6. **Унифицированные кнопки:** [📋 Копировать] [🔄 Ещё вариант] [🏠 Меню]
7. Дополнительно: [✂️ Короче] [📄 Длиннее]

### 4.3 Помощь с перепиской

**Сценарии использования:**
- Клиент не отвечает — напомнить мягко
- Подчинённый накосячил — написать без демотивации
- Холодное сообщение в личку — не выглядеть спамом
- Клиент недоволен — сгладить конфликт
- Нужно попросить об оплате — деликатно

**Флоу с индикаторами шагов:**
1. Кнопка "💬 Помощь с перепиской"
2. **Шаг 1 из 3:** "Опишите ситуацию" + кнопки [😶 Клиент молчит] [😤 Конфликт] [💰 Про оплату]
3. **Шаг 2 из 3:** "Чего хотите добиться?"
4. **Шаг 3 из 3:** Выбор тона: [😊 Мягко] [😐 Нейтрально] [😤 Настойчиво]
5. Генерация (typing indicator)
6. **Унифицированные кнопки:** [📋 Копировать] [🔄 Ещё вариант] [🏠 Меню]
7. Модификаторы: [😊 Мягче] [😤 Жёстче]

**Результат:** Короткое сообщение 2-4 предложения, без формальностей.

### 4.4 Генератор идей (5 → +3)

**Флоу:**
1. Кнопка "💡 Генератор идей"
2. Кнопки быстрого выбора: [📈 Увеличить доход] [👥 Найти клиентов] [📱 Идеи контента] [Другое...]
3. Ввод задачи
4. Генерация 5 идей
5. Унифицированные кнопки: [📋 Копировать] [💡 Ещё 3 идеи] [🏠 Меню]
6. Если "Ещё" → генерация +3 новых идей

### 4.5 Улучшение текста

**Стили улучшения:**
- Более убедительно
- Короче и чётче
- Более формально
- Более дружелюбно

**Флоу:**
1. Кнопка "Улучшить текст"
2. "Отправьте текст для улучшения"
3. Выбор стиля
4. Генерация → Результат

**Критерии завершения Фазы 4:**
- [ ] Главное меню с 4 кнопками работает
- [ ] КП-генератор: 3 шага с индикаторами, унифицированные кнопки
- [ ] Переписка: 3 шага, выбор тона, унифицированные кнопки
- [ ] Генератор идей: 5 идей → +3, унифицированные кнопки
- [ ] Улучшение текста: выбор стиля, унифицированные кнопки
- [ ] Все кнопки результата единообразны: [📋] [🔄] [🏠]

---

## Фаза 5: Лимиты и polish (День 10-11)

### 5.1 Система лимитов

| Уровень | Лимит | Реализация |
|---------|-------|------------|
| Free | **15 запросов/день** | Счётчик в модели User (дать распробовать!) |
| Pro | 100 запросов/день | Для подписчиков |
| MVP-тестеры | Без лимитов | Для первых тестировщиков (по флагу)

### 5.2 Сообщения об ошибках

- Превышен лимит
- Ошибка OpenAI
- Слишком длинный текст
- Пустой ввод

### 5.3 UX улучшения

- Индикатор "печатает..." при генерации
- Форматирование результата (markdown)
- Быстрые действия после генерации

**Критерии завершения Фазы 5:**
- [ ] Лимиты работают
- [ ] Ошибки обрабатываются gracefully
- [ ] UX приятный и понятный

---

## Риски

| Риск | Вероятность | Влияние | Митигация |
|------|-------------|---------|-----------|
| Низкое качество промптов | Высокая | Высокое | Итерации, тестирование на реальных задачах |
| Медленная генерация | Средняя | Среднее | Streaming, индикатор загрузки |
| Лимиты OpenAI | Низкая | Высокое | Rate limiting, очередь запросов |
| Сложный UX флоу | Средняя | Среднее | Тестирование на пользователях |

---

## Стратегия отката

1. **Проблемы с БД:** Откат миграций через Aerich
2. **Проблемы с ботом:** `git reset --hard` + перезапуск
3. **Проблемы с Docker:** `docker-compose down -v` + пересоздание
4. **Проблемы с OpenAI:** Fallback на mock-ответы для тестирования

---

## Критерии завершения MVP

### Must Have (без этого не запускаем):
- [ ] Бот запускается и работает стабильно
- [ ] /start сразу показывает меню (отложенный онбординг)
- [ ] 📝 КП-генератор работает (3 шага + индикаторы)
- [ ] 💬 Помощь с перепиской работает (3 шага)
- [ ] 💡 Генератор идей работает (5 → +3)
- [ ] ✨ Улучшение текста работает
- [ ] Унифицированные кнопки: [📋 Копировать] [🔄 Ещё] [🏠 Меню]
- [ ] Лимит 15 запросов/день

### Nice to Have (если успеем):
- [ ] Отложенный онбординг (триггер после 2-3 генераций)
- [ ] Кнопки быстрого выбора (сценарии)
- [ ] Модификаторы результата (короче/длиннее, мягче/жёстче)
- [ ] Typing indicator при генерации

---

## Timeline

| День | Фаза | Результат |
|------|------|-----------|
| 1-2 | Структура | Проект настроен, зависимости работают |
| 3-4 | Базовый бот | /start, онбординг, меню |
| 5-6 | AI-сервис | Генерация через OpenAI работает |
| 7-9 | Фичи | Все 4 фичи работают |
| 10-11 | Polish | Лимиты, UX, тестирование |

**Общий срок:** 11 рабочих дней (~2.5 недели)

---

**Статус:** ⏳ Ожидает утверждения  
**Автор:** AI Agent  
**Дата:** декабрь 2024  
**Версия:** 3.0 — синхронизировано с UX-рекомендациями
